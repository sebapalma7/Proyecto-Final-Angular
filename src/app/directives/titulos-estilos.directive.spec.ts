import { TitulosEstilosDirective } from './titulos-estilos.directive';

describe('TitulosEstilosDirective', () => {
  it('should create an instance', () => {
    const directive = new TitulosEstilosDirective();
    expect(directive).toBeTruthy();
  });
});
